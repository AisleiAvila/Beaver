spring:
  application:
    name: empresa
  datasource:
    url: jdbc:postgresql://localhost:5432/empresa
    username: postgres
    password: admin
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
  security:
    user:
      password: admin
  main:
    allow-bean-definition-overriding: true
  mvc:
    cors:
      mappings:
        "/**":
          allowed-origins: "http://localhost:4200, http://localhost:8100, http://localhost:8080"
          allowed-methods: "*"
          allowed-headers: "*"
          allow-credentials: true
  thread:
    virtual:
      enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: aisleiavilademedeiros@gmail.com
    password: bnuw rjxr dqhf xshc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

api:
  security:
    token:
      secret: my-secret-key-loja

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    operationsSorter: alpha

security:
  excludedUrls: /auth/login,/auth/register,/auth/authenticate,/senha/recuperar,/senha/validar-reset-token,/usuario/perfil,/organizacao/find,/documentation.html,/favicon.ico,/swagger-ui/**,/v3/api-docs/**,"/api/public/**"

logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"
  level:
    org.jooq: DEBUG
    org.springframework: DEBUG

output:
  ansi:
    enabled: always

llama:
  url: http://localhost:11434/api/generate